    /** @return the audit event severity level **/
    public SeverityLevel getSeverityLevel()
    {
        return message == null
            ? SeverityLevel.INFO
            : message.getSeverityLevel();
    }

