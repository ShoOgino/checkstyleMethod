    /** {@inheritDoc} */
    @Override
    public boolean accept(AuditEvent event)
    {
        final boolean result = severityLevel == event.getSeverityLevel();
        if (acceptOnMatch) {
            return result;
        }
        return !result;
    }

