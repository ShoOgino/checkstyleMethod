    @Override
    public DetailNode[] getChildren()
    {
        if (children == null) {
            return new DetailNode[0];
        }
        else {
            return Arrays.copyOf(children, children.length);
        }
    }

