        /**
         * Set true, if import is static
         * @param isStatic
         *        if import is static.
         */
        public void setStatic(boolean isStatic)
        {
            this.isStatic = isStatic;
        }

