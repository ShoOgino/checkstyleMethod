        /**
         * Creates new member context.
         * @param ast member AST node.
         */
        public Context(DetailAST ast) {
            this.ast = ast;
            count = 0;
        }

