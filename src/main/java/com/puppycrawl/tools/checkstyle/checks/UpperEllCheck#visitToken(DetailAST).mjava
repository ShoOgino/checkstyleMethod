    @Override
    public void visitToken(DetailAST ast)
    {
        if (ast.getText().endsWith("l")) {
            log(ast.getLineNo(),
                ast.getColumnNo() + ast.getText().length() - 1,
                "upperEll");
        }
    }

