    @Override
    public void endElement(final String namespaceURI, final String localName,
        final String qName) {
        if ("subpackage".equals(qName)) {
            stack.pop();
        }
    }

