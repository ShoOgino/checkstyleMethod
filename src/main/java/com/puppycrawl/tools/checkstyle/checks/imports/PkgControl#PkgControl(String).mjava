    /**
     * Construct a root node.
     * @param pkgName the name of the package.
     */
    PkgControl(final String pkgName) {
        assert pkgName != null;
        parent = null;
        fullPackage = pkgName;
    }

