    @Override
    public void finishTree(DetailAST ast) {
        if (!defined) {
            if (ast == null) {
                log(DEFAULT_LINE_NUMBER, MSG_KEY_MISSING);
            }
            else {
                log(ast, MSG_KEY_MISSING);
            }
        }
    }

