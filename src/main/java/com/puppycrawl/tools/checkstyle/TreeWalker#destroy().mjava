    @Override
    public void destroy() {
        for (AbstractCheck check : ordinaryChecks) {
            check.destroy();
        }
        for (AbstractCheck check : commentChecks) {
            check.destroy();
        }
        super.destroy();
    }

