    /**
     * Helper for testing.
     * @return the package names that have been added
     */
    String[] getPackages()
    {
        return mPackages.toArray(new String[mPackages.size()]);
    }

