    /** @return the first line of the method signature **/
    int getFirstLineNo()
    {
        return (mModSet.size() > 0)
            ? mModSet.getFirstLineNo()
            : mName.getLineNo();
    }

