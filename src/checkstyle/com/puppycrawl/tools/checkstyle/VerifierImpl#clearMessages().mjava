    /** @see Verifier **/
    public void clearMessages()
    {
        mLines = null;
        mPkgName = null;
        mInInterface.clear();
        mInScope.clear();
        mMessages.clear();
        mComments.clear();
        mImports.clear();
        mReferenced.clear();
    }

