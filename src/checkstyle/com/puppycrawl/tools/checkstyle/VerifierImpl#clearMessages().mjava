    /** @see Verifier **/
    public void clearMessages()
    {
        mLines = null;
        mPkgName = null;
        mInInterface.clear();
        mMethodBlockLevel = 0;
        mMessages.clear();
        mComments.clear();
        mImports.clear();
        mReferenced.clear();
    }

