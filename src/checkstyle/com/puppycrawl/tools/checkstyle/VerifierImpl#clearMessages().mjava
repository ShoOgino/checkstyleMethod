    /** @see Verifier **/
    public void clearMessages()
    {
        mInInterface.clear();
        mMethodBlockLevel = 0;
        mMessages.clear();
        mComments.clear();
        mImports.clear();
        mReferenced.clear();
    }

