    /** {@inheritDoc} */
    public void process(File[] aFiles)
    {
        final File[] javaFiles = filter(aFiles);

        for (File element : javaFiles) {
            process(element);
        }
    }

