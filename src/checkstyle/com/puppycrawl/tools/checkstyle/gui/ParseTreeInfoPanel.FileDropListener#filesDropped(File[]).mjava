        public void filesDropped(File[] files)
        {
            if ((files != null) && (files.length > 0))
            {
                File file = files[0];
                openFile(file, mSp);
            }
        }

