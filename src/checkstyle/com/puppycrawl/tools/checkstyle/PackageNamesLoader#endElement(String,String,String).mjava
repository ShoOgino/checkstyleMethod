    @Override
    public void endElement(String aNamespaceURI,
                           String aLocalName,
                           String aQName)
    {
        if ("package".equals(aQName)) {

            mPackageNames.add(getPackageName());
            mPackageStack.pop();
        }
    }

