    /** Resets the verifier. Use before processing a file. **/
    void reset()
    {
        mLines = null;
        mInInterface.clear();
        mMessages.reset();
        mMethodBlockLevel = 0;
    }

