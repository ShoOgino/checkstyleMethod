    /** @return whether the method is a function **/
    boolean isFunction()
    {
        return isConstructor()
            ? false
            : !"void".equals(mReturnType.getText().trim());
    }

