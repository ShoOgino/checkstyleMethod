    /** {@inheritDoc} */
    public void beginTree(DetailAST aRootAST)
    {
        super.beginTree(aRootAST);
        mCollect = false;
        mImports.clear();
        mReferenced.clear();
    }

