        /**
         * Creates new member context.
         * @param aAST member AST node.
         */
        public Context(DetailAST aAST)
        {
            mAST = aAST;
            mCount = 0;
        }

