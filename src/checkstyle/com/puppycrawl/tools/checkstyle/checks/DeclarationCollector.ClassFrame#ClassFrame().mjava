        /**
         * Creates new instance of ClassFrame
         */
        public ClassFrame()
        {
            super();
            mInstanceMembers = Sets.newHashSet();
            mStaticMembers = Sets.newHashSet();
        }

