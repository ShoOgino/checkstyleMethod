    /**
     * {@inheritDoc}
     */
    public void checkMethods(DetailAST aAST)
    {
        mEjbCreates.clear();
        mEjbPostCreates.clear();

        super.checkMethods(aAST);

        checkCreateMatch();
    }

