    /**
     * Constructs a method checker for a local home interface check.
     * @param aCheck the local home interface check.
     */
    public LocalHomeInterfaceMethodChecker(LocalHomeInterfaceCheck aCheck)
    {
        super(aCheck);
    }

