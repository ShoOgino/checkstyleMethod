        /**
         * Leave a scope, i.e. pop a frame from the stack.
         * @return the left frame
         */
        LexicalFrame leave()
        {
            return mFrameList.removeFirst();
        }

