        public int getLevelImpl() 
        {
            if (isElseIf()) {
                return mParent.getLevel();
            } 
            else {
                return super.getLevelImpl();
            }
        }

