    /**
     * Determines if the top level token must start the line.
     *
     * @return false
     */
    @Override
    protected boolean toplevelMustStartLine()
    {
        return false;
    }

