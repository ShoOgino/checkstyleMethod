    /**
     * @return true if a given method is an equals().
     * @param aAST method definition to check.
     */
    private boolean isEqualsMethod(DetailAST aAST)
    {
        return aAST.findFirstToken(TokenTypes.IDENT).getText()
            .equals(EQUALS_METHOD_NAME);
    }

