        /**
         * Creates a new <code>FieldStats</code> instance.
         *
         * @param aName name of the field
         */
        FieldStats(String aName)
        {
            mName = aName;
        }

