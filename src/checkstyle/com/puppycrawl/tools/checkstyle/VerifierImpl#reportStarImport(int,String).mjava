    /** @see Verifier **/
    public void reportStarImport(int aLineNo, String aPkg)
    {
        if (mCheckImports) {
            log(aLineNo, "Avoid using the '.*' form of import.");
        }
    }

