    /** {@inheritDoc} */
    public boolean accept(AuditEvent aEvent)
    {
        for (Filter filter : mFilters) {
            if (!filter.accept(aEvent)) {
                return false;
            }
        }
        return true;
    }

