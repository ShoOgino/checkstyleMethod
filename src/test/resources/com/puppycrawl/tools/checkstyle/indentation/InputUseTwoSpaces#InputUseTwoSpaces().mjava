  /** Creates a new instance of InputUseTabs */
  public InputUseTwoSpaces() {
    boolean test = true;
    if (test)
    {
      while (
        test == false) {
        System.exit(2);
      }
    }
     System.exit(3);
  }

