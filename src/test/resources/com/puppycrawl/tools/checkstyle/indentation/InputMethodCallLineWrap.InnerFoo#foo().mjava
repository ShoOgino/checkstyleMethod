    	void foo() {
            new String()
                .substring(
                    0, 100
                )
                .substring(
                    0, 50
                );
        }

