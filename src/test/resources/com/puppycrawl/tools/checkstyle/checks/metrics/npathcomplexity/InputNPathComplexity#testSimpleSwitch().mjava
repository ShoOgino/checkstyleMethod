    //NP = 4
    int testSimpleSwitch() {
        int a = 0;
        // NP = (case-range[1]=1) + (case-range[2]=1) + (case-range[3]=1)
        //         + (default-range=1) + (expr=0) = 4
        switch(a) {
        case 1:
            break;
        case 2:
        case 3:
            break;
        }
        return a;
    }

