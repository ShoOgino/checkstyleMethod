    private void method(){
        if(false) {
            switch (0) {
                case -1: // violation
                    return;
                default: // violation
                    return;
            }
        }
        switch(1){
            case 1: return;
            default: throw new RuntimeException("");
        }
    }

