    void testDo() {
        //Valid
        do {
            testDo();
        } while (condition());

        //Invalid
        do{                             //violation
            testDo();
        } while (condition());
    }

