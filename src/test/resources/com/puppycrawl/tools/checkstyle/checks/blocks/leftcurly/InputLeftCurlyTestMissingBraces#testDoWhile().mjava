    /** Test do/while loops **/
    void testDoWhile()
    { // violation
        // Valid
        do {
            testDoWhile();
        }
        while (condition());

        // Invalid
        do testDoWhile(); while (condition());
    }

