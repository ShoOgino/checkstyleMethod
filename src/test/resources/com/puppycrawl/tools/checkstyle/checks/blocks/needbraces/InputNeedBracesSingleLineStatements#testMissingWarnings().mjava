    private int testMissingWarnings() {
        if (true) // violation
            throw new RuntimeException();
        if (true) {
            return 1;
        } else // violation
            return 2;
    }

