    void foo2() {
        int a;
        if (true) {
        }
        else {
            a = 1;

            try {
            } catch (Exception e) {
            } finally {
            }

            if (true) {
            }

            Stream.of(1).forEach(integer -> {});

            synchronized (InputFinalLocalVariableLeavingSlistToken.class) {
            }

            for (int i = 0; i < 0; ++i) {
            }
            while (Math.random() > 0) {
            }
            do {
            }
            while (Math.random() > 0);

            {}

            a = 2;
        }
    }

