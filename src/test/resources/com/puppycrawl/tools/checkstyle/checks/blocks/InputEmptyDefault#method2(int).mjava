    void method2(int a) {
        switch (a) {
            case 1:a++;
            case 2:a++;
            default:             // no violation
                switch (a) {
                    default: {   // violation if checking for statements

                    }
                }
        }
    }

