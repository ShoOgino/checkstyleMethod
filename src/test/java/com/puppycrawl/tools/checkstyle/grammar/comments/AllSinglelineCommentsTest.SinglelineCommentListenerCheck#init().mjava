        @Override
        public void init() {
            final int lines = 63;
            for (int i = 0; i < lines; i++) {
                ALL_COMMENTS.add(i + lineSeparator);
            }
            ALL_COMMENTS.add(String.valueOf(lines));
        }

