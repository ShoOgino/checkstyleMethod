        @Override
        public void init() {
            int lines = 63;
            for (int i = 0; i < lines; i++) {
                allComments.add(i + LINE_SEPARATOR);
            }
            allComments.add(String.valueOf(lines));
        }

