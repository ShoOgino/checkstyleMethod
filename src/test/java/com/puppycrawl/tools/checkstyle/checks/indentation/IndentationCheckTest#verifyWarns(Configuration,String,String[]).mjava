    private void verifyWarns(Configuration config, String filePath,
                    String[] expected)
                    throws Exception {
        verifyWarns(config, filePath, expected, 0);
    }

