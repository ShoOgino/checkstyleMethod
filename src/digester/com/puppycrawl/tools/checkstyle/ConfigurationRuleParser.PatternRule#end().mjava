        /**
         * If there was a pattern for this element, pop it off the pattern
         * stack.
         */
        public void end()
        {
            if (mPattern != null) {
                patternStack.pop();
            }
        }

