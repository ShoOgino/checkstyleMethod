    private void foo2() {
        if (true) {
            /* some */
            int k = 0;
                /* // warn */
            int b = 10;
                /* // warn
                 * */
            double d; /* trailing comment */
                /* // warn
             *
                */
            boolean bb;
            /***/
            /* my comment*/
            /*
             *
             *
             *  some
             */
            /*
             * comment
             */
            boolean x;
        }
    }

