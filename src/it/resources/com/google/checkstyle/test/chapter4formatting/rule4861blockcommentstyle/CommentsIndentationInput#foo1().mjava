  private void foo1() {
    if (true) {
    /* ok (some) */
    int k = 0;
        /* // warn (some) */
    int b = 10;
        /* // warn
         * */
    double d; /* ok (trailing comment) */
        /* // warn
     *
        */
    boolean bb;
    /***/
    /* ok (my comment) */
    /*
     *
     *
     * ok (some)
     */
    /*
     * ok (comment)
     */
    boolean x;
    }
  }

