  private void foo() {
    if (true) {
      // ok (here initialize some variables)
      int k = 0; // ok (trailing comment)
        // warn (initialize b)
      int b = 10;
      // ok (sss)
    }
  }

