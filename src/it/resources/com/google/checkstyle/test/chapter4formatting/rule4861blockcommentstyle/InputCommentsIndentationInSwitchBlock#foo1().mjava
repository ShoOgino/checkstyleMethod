    private static void foo1() {
        if (true) {
            switch(1) {
                case 0:

                case 1:
                        // warn
                    int b = 10;
                default:
                 // comment
            }

        }
    }

