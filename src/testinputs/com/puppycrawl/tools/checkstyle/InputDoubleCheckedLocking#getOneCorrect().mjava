    private static Integer getOneCorrect()
    {
        synchronized (InputDoubleCheckedLocking.class)
        {
            if (one == null)
            {
                one = new Integer(1);
            }
        }
        return one;
    }

