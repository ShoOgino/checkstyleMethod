  /** Creates a new instance of InputUseTabs */
  public InputUseTabs() {
    boolean test = true;
    if (test)
    {
      while (
        test == false) {
        System.exit(2);
      }
    }
     System.exit(3);
  }

