    public void  parenIfTest() {
        boolean test = true;

        if (test
        ) {
            System.getProperty("blah");
        }
//
        if (test
        )
        {
            System.getProperty("blah");
        }

        if
        (
            test
        ) {
            System.getProperty("blah");
        }

        if (test
            )
        {
        }
    }

